extends ../layout

block content
  h1= title
  p Welcome to #{title}

  ul
    each level in levels
      li
        = level.name
        pre
          = JSON.stringify(JSON.parse(level.source), null, 4)

